#user_navigation
  - if current_user
    Hello,
    = current_user.email
    |
    = link_to 'Sign out', '/users/sign_out'
  - else
    =link_to 'Login', '/users/sign_in'
    |
    =link_to 'Register', '/users/sign_up'